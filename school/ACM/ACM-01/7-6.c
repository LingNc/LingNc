#include<stdio.h>
#include<math.h>
int main(){
    long long int n0,n1,n;
    scanf("%d %d %d",&n0,&n1,n);
    double z=n0+n1+2*sqrt(n0*n1);

    n0/x-n1/y=z
    x+y=n
    y=n-x;
    z=(n0/x-n1/y)(x+y)*1/n>=(sqrt(n0)+sqrt(n1))^2=min(z);
    n0+n1+2sqrt(n0*n1)
    min(z);
    return 0;
}