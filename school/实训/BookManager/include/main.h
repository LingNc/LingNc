#ifndef _std_lib
#define _std_lib
#include<iostream>
#include<fstream>
#include<string>
#endif

#ifndef _std_using
#define _std_using
using string=std::string;
using fstream=std::fstream;
#endif

#ifndef _my_def
#define _my_def
#define foe(i,a,b) for(auto i=(a);i<=(b);i++)
#endif